# 알파에이전트 저장소 — 작업 메모

## 언어 원칙
사용자에게 결과물을 보여주거나 제안할 때는 한국어로 응답한다(2026-07-16 확정). 다만 출처(Sources) 섹션의 논문·글 제목·URL 등 실제 자료 자체는 원문 언어 그대로 표기한다 — 번역하면 원 출처를 찾기 어려워지므로.

## 외부 저장 위치 (이 git 저장소 밖, 사용자 로컬 PC)

이 저장소 외에도 사용자가 로컬 PC에 관련 자료를 보관하는 위치가 있다. **새 자료를 만들거나 "이런 게 있나?" 찾을 때, 사용자에게 매번 묻지 말고 먼저 아래 경로를 확인한다:**

- `D:\에이전트자료저장고` — GitHub 저자별 참고자료 스냅샷. `.git` 없음, 이 저장소와 중복 없음.
- `E:\안티그라비티 자료` — 대부분 마찬가지 참고자료 스냅샷. **단 그 안의 `brain/`이 바로 이 저장소(`nowhere78/home`)의 실제 로컬 git 작업 폴더이고, `알파에이전트/`는 별도 저장소 `Nowhere78/apar`의 작업 폴더다.** 둘 다 `.git` 있는 live 저장소 — 단순 참고자료로 취급하지 말 것 (2026-06-22 확인).
- `C:\Users\smile\알파에이전트\Knowledge_Base\_company\00_Raw\reports` — 2026-06-22 확인 결과 이 머신에는 존재하지 않음(`C:\Users\smile\알파에이전트`에는 소설 관련 파일만 있음). src/automation/research_task.py가 참조하는 경로라면 다른 머신/환경 기준일 수 있으니 실제 사용 전 재확인할 것.

> 클라우드/샌드박스 세션(Claude Code on the web 등)에는 위 드라이브가 마운트되어 있지 않아 직접 읽을 수 없다. 그런 세션에서는 추측하지 말고 "로컬 PC 세션이 아니라 이 경로는 못 확인했다"고 명확히 말한다. 사용자의 로컬 머신에서 실행되는 세션에서는 바로 확인하고 작업에 반영한다.
>
> ⚠️ **시크릿 주의**: `E:\안티그라비티 자료\brain`(이 저장소의 로컬 작업 폴더) 안의 에이전트 세션 로그(`*/.system_generated/logs/transcript.jsonl` 등)는 auto-sync로 커밋되는데, 2026-06-22에 그 안에 OpenAI API 키가 평문으로 들어가 GitHub push protection에 걸린 적이 있다. push 전에 시크릿이 섞이지 않았는지 주의할 것.

## 알려진 폴더 중복 — 새 파일 쓰기 전에 확인

이 저장소 안에서도 이름이 비슷한 폴더가 여러 군데로 갈라져 있다. 어디가 "진짜 최신"인지 확인 없이 새로 쓰면 데이터가 더 쪼개진다.

- `_company/_agents/*/memory.md` — ✅ 실제 운영 중인 에이전트 메모리 (가장 크고 최신, 예: `ceo/memory.md` 150KB+)
- `_agents/*/{goal,prompt}.md` — 페르소나·목표 템플릿. 수정하면 다음 자율 사이클부터 반영됨
- `Knowledge_Base/_company/` — 예전 스냅샷, 대부분 비어있음. 새 내용을 여기 쓰지 않는다

탐색 시작점은 [Home.md](Home.md) (옵시디언 vault로 열 때 진입점 노트).

## 작업 원칙
- 새 자료를 추가하기 전에 (1) 이 저장소 내 비슷한 폴더, (2) 로컬 PC의 `D:\`, `E:\` 저장 위치에 이미 비슷한 게 있는지 먼저 확인하고, 있으면 합치거나 참조한다 — 중복 생성 금지.
- 중복/혼선을 새로 발견하면 이 파일에 그 사실을 추가해서 다음 세션이 또 같은 걸 묻지 않게 한다.

## NotebookLM MCP 연동 (2026-07-16 설치)

이 저장소 루트의 `.mcp.json`에 `notebooklm` MCP 서버(https://github.com/PleasePrompto/notebooklm-mcp)가 등록되어 있다. 자격증명은 포함되어 있지 않아 커밋해도 안전함.

- **동작 방식**: 로컬 Chrome을 띄워 구글 계정으로 로그인하고, 세션 쿠키를 로컬 프로필 디렉토리에 저장해 재사용. 클라우드/샌드박스 세션에서는 대화형 브라우저 로그인을 완료할 방법이 없어 사실상 못 쓴다 — **로컬 PC에서 실행되는 Claude Code 세션에서만 유효.**
- **최초 1회 인증**: 로컬 세션에서 `setup_auth` 도구(또는 최초 실행 시 자동 트리거)를 통해 사용자가 직접 구글 로그인 완료해야 함. 클라우드 세션이 대신 해줄 수 없음.
- **주의**: 세션 쿠키는 암호화 없이 로컬에 저장됨(`~/.local/share/notebooklm-mcp/chrome_profile/` 등, OS별 경로 다름). 이 경로를 git에 커밋하지 않도록 주의.
- 클라우드 세션에서 노트북LM 연동을 물어보면 "설정은 되어 있으나 로컬 세션에서 인증을 완료해야 쓸 수 있다"고 안내할 것.

## Playwright MCP 연동 (2026-07-17 설치)

이 저장소 루트의 `.mcp.json`에 `playwright` MCP 서버(공식 `@playwright/mcp`, Microsoft — https://github.com/microsoft/playwright-mcp)가 등록되어 있다. 자격증명 없음, 커밋해도 안전함.

- **동작 방식**: `npx @playwright/mcp@latest`로 실행. 브라우저(기본 Chromium)를 띄워 접근성 트리(accessibility snapshot) 기반으로 페이지를 탐색·조작한다. 스크린샷·클릭·입력·폼 제출 등 웹 자동화를 도구로 노출.
- **로컬 vs 클라우드**: 로컬 PC 세션에서는 로컬 브라우저를 그대로 쓴다. 클라우드/샌드박스 세션에도 Chromium이 사전 설치되어 있어(`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`) 원칙적으로 동작 가능하나, 헤드리스 환경 제약이 있을 수 있음. `playwright install`은 실행하지 말 것(이미 설치됨).
- notebooklm과 달리 별도 로그인/인증 절차는 필요 없다.

## 설교문 작성 가이드라인 (다른 세션에서도 적용할 것)

사용자가 설교문 작성/수정을 요청하면 아래를 기본 원칙으로 적용한다. 특별히 다른 방향을 요청하지 않는 한 매번 다시 설명하지 말고 바로 반영한다.

**해석 노선 (2026-07-16, 마 5:38-42 작업에서 확정)**
- 종말론적 성품 윤리 중심(Davies-Allison, France, Carson 계열) + 능동적 비보복(윙크의 "능동태 명령" 관찰은 부분 수용, "권력 구도 뒤집기" 프레임은 중심에 두지 않음) + 그리스도 중심 독법(본문을 그리스도의 자화상/십자가로 연결).
- 절대평화주의(톨스토이류) 해석은 기본으로 채택하지 않되, 원하면 각주로 소개 가능.
- 폭력·학대 피해자에게 본문을 "참으라"는 뜻으로 오용하지 않도록 적용부에 반드시 안전 단서를 넣는다(특히 중고등부 대상 시 학교폭력은 알려야 한다는 문장 필수).

**전달 기법 — Patrick Winston, MIT "How to Speak" 강연에서 가져온 원칙 (2026-07-16 유튜브 링크 검토 후 채택)**
1. **오프닝 약속(Promise)**: 서론 끝에서 "이 말씀을 들으면 청중이 무엇을 얻어갈지"를 명시적 한 문장으로 선언한다.
2. **사이클(Cycle)**: 핵심 메시지(제목 문장, 후렴구)를 최소 세 번, 형태를 바꿔가며(말로/글로 요약 문구로) 반복한다. 발표 화면이나 인쇄물에 후렴구를 시각적으로도 노출할 것을 제안한다.
3. **Empowerment**: 적용부는 항상 청중이 실제로 들고 갈 수 있는 구체적 실천 항목(2-3개)으로 마무리한다. 감동으로만 끝내지 않는다.
4. **강한 클로징 한 줄**: 마지막은 청중이 그대로 인용할 수 있는 짧고 대구를 이루는 문장(예: "A는 ~하는 길이고, B는 ~하는 길입니다")으로 맺는다.
5. 즉흥 유머보다 설교자 자신의 경험담(자기 노출형)을 웃음 포인트로 권장.

**작업 이력**
- `설교/마태복음5장38-42_반응하는사람_결단하는사람.md` — 전 세대 통합 40분 버전(장년+중고등부, "아이들을 위한 한마디" 보충란 포함). 이전 2편(장년용/중고등부용 분리본)은 이 파일로 대체되어 삭제됨.
- 브랜치: `claude/matthew-5-38-42-commentary-bn94qd`.
